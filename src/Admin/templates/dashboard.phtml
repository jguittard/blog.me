<?php
/** @var int $postCount */
/** @var int $categoryCount */
/** @var int $tagCount */
$this->headTitle('Dashboard');

$cards = [
    ['Posts', $postCount, $this->url('admin.posts.index')],
    ['Categories', $categoryCount, $this->url('admin.categories.index')],
    ['Tags', $tagCount, $this->url('admin.tags.index')],
];
?>
<div class="space-y-6">
    <div class="flex items-center justify-between">
        <h1 class="text-xl font-bold tracking-tight">Dashboard</h1>
        <a
            href="<?= $this->url('admin.posts.create') ?>"
            class="rounded-lg bg-primary px-3 py-2 text-sm font-medium text-white hover:bg-blue-700"
        >+ New post</a>
    </div>

    <div class="grid gap-4 sm:grid-cols-3">
        <?php foreach ($cards as [$label, $count, $href]): ?>
            <a href="<?= $href ?>" class="rounded-lg border border-gray-200 bg-white p-5 hover:shadow-sm">
                <div class="text-3xl font-semibold"><?= (int) $count ?></div>
                <div class="mt-1 text-sm text-gray-500"><?= $this->escapeHtml($label) ?></div>
            </a>
        <?php endforeach; ?>
    </div>
</div>
