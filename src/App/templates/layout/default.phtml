<?php
$this->headTitle('blog.me')->setSeparator(' · ')->setAutoEscape(false);
?>
<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <?= $this->headTitle() ?>
    <?= $this->headMeta() ?>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
        rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: { primary: '#2563eb' },
                    fontFamily: {
                        sans: ['Inter', 'ui-sans-serif', 'system-ui', 'sans-serif'],
                    },
                },
            },
        };
    </script>
</head>
<body class="flex min-h-full flex-col bg-gray-50 font-sans text-gray-900 antialiased">
    <header class="sticky top-0 z-10 border-b border-gray-200 bg-white/80 backdrop-blur">
        <div class="mx-auto flex max-w-3xl items-center justify-between px-4 py-4">
            <a href="<?= $this->url('blog.list') ?>" class="text-lg font-semibold tracking-tight">
                blog<span class="text-primary">.</span>me
            </a>
            <nav class="flex gap-6 text-sm font-medium text-gray-600">
                <a class="transition-colors hover:text-gray-900" href="<?= $this->url('blog.list') ?>">Posts</a>
                <a class="transition-colors hover:text-gray-900" href="<?= $this->url('blog.categories') ?>">Categories</a>
            </nav>
        </div>
    </header>

    <main class="mx-auto w-full max-w-3xl flex-1 px-4 py-10">
        <?= $this->content ?>
    </main>

    <footer class="mx-auto w-full max-w-3xl px-4 py-10 text-sm text-gray-400">
        <p>PPL lessons &amp; aircraft notes &middot; <?= date('Y') ?></p>
    </footer>
</body>
</html>
