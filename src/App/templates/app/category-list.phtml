<?php
/** @var \App\Domain\Entity\Category[] $categories */
$this->headTitle('Categories');
?>
<div class="space-y-8">
    <header>
        <h1 class="text-2xl font-bold tracking-tight">Categories</h1>
        <p class="mt-1 text-sm text-gray-500">Browse posts by PPL theory subject.</p>
    </header>

    <div class="grid gap-4 sm:grid-cols-2">
        <?php foreach ($categories as $category): ?>
            <a
                href="<?= $this->url('blog.category', ['slug' => (string) $category->slug]) ?>"
                class="block rounded-lg border border-gray-200 bg-white p-5 transition-shadow hover:shadow-sm"
            >
                <h2 class="font-semibold tracking-tight"><?= $this->escapeHtml($category->name) ?></h2>
                <?php if ($category->description !== null): ?>
                    <p class="mt-1 text-sm leading-6 text-gray-600"><?= $this->escapeHtml($category->description) ?></p>
                <?php endif; ?>
            </a>
        <?php endforeach; ?>
    </div>
</div>
